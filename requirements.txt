# =============================================================================
# SSDLC Multi-Agent Platform — Dependencies
# Phase 0 requirements (all installed natively in .venv)
# =============================================================================

# --- LLM & Agent Framework ---
langchain>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0

# --- LLM SDKs (Phase 0: Ollama only; keys used for Phase 1+ cloud fallback) ---
anthropic>=0.40.0
openai>=1.0.0
httpx>=0.27.0                   # Ollama API calls

# --- PostgreSQL + pgvector ---
asyncpg>=0.29.0                 # async PostgreSQL driver
psycopg2-binary>=2.9.0          # sync driver (migrations, scripts)

# --- Embeddings (UniXcoder for pgvector) ---
transformers>=4.40.0            # HuggingFace UniXcoder
torch>=2.0.0                    # Required by transformers (MPS backend on Apple Silicon)
numpy>=1.24.0

# --- API Server ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
jinja2>=3.1.0                   # HTML template rendering for labeling UI
aiofiles>=23.0.0                # async static file serving

# --- Config & Utilities ---
python-dotenv>=1.0.0
pyyaml>=6.0.0                   # YAML config, prompt files, FP rules
tenacity>=8.2.0                 # Retry logic for LLM calls

# --- Security Tool Wrappers ---
# Semgrep, Grype, Checkov run in Docker — no Python packages needed here

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                   # FastAPI TestClient async support
